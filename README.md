# PDF combiner print xd

How to run :)

* Put files to be combined in cards folder
* Put cardback in the cardback folder
* run `npm install`
* run `npm start`

If mergin JPGS to an image use 
```
node jpg.js
```
Note, only works if all the files are Images/PDF (ONE TYPE ONLY)